name: Release binaries

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-24.04
            architecture_pattern: "x86-64"
          - name: linux-aarch64
            os: ubuntu-24.04-arm
            architecture_pattern: "ARM aarch64"
          - name: macos-x86_64
            os: macos-15-intel
            architecture_pattern: "x86_64"
          - name: macos-aarch64
            os: macos-15
            architecture_pattern: "arm64"

    steps:
      - uses: actions/checkout@v4
      - uses: bazelbuild/setup-bazelisk@v3
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/bazel
            ~/.cache/bazelisk
          key: bazel-9-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.bazelversion', 'MODULE.bazel', 'MODULE.bazel.lock', 'Cargo.lock', '**/*.bzl') }}
          restore-keys: bazel-9-${{ runner.os }}-${{ runner.arch }}-
      - name: Test
        run: bazel test //:rq_tui_tests --lockfile_mode=error
      - name: Build
        run: bazel build //:rq-tui --compilation_mode=opt --lockfile_mode=error
      - name: Verify binary architecture
        shell: bash
        run: file bazel-bin/src/rq-tui | grep -E '${{ matrix.architecture_pattern }}'
      - name: Package binary
        shell: bash
        run: |
          mkdir -p dist
          cp "bazel-bin/src/rq-tui" "dist/rq-tui-${{ matrix.name }}"
          tar -C dist -czf "rq-tui-${{ matrix.name }}.tar.gz" "rq-tui-${{ matrix.name }}"
          shasum -a 256 "rq-tui-${{ matrix.name }}.tar.gz" > "rq-tui-${{ matrix.name }}.tar.gz.sha256"
      - uses: actions/upload-artifact@v4
        with:
          name: rq-tui-${{ matrix.name }}
          path: |
            rq-tui-${{ matrix.name }}.tar.gz
            rq-tui-${{ matrix.name }}.tar.gz.sha256

  publish:
    name: Publish GitHub release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: rq-tui-*
          merge-multiple: true
      - name: Publish release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "${{ github.ref_name }}" dist/* --repo "${{ github.repository }}" --generate-notes
