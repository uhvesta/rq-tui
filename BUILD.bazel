load("@rules_rust//rust:defs.bzl", "rust_binary")
load("@crates//:defs.bzl", "crate_deps")

rust_binary(
    name = "rq-tui",
    srcs = ["src/main.rs"],
    edition = "2021",
    deps = crate_deps(["crossterm", "ratatui"]),
)
